import 'package:flutter/material.dart';

class AgeCategory14_16Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('14-16 Age Category'),
      ),
      body: Center(
        child: Text(
          'Welcome to the 14-16 age category!',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
